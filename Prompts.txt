Change the wording on the frontend for "AI Insights", Keep this "Design Gap Analyis".

And currently in our project there are three types of AI summary generation modules. Please take the "AI Insights" as standard and redesign the other two according to that. Means the UI, filters, the summary genearion, etc. 

I know those two are running on Local LLM API so keep it as it is. But change the working of them. I want same UI and same way of generation of AI summary. No need to add reports in those two.

Ask if I am missing something to define.