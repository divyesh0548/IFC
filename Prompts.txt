For timestamo issue I have full scalable solution.

First of all change all timestamps in website to this -->  DateTime @default(now())

Fetch the timestamp without any conversion from DB.

In frontend for each timestamps use toLocaleString() method, which will convert the time in local time of the client location.

This is just for time conversion, keep the formatting intact as in different places, timestamp displays differetly.