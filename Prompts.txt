Changes for API call optimization.

1. Add an auth context using context API. So all components will have shared access to user identity and role. So on each API call it will not call verify api again. 
Currently the app loads 
✅ Token verified successfully, user: divyesh.parmar@sharpandtannan.com role: company_co
✅ Company coordinator verified successfully, user: divyesh.parmar@sharpandtannan.com role: company_co
✅ Company coordinator verified successfully, user: divyesh.parmar@sharpandtannan.com role: company_co

this 5-6 times when loading some data.

2. Keep RoleBasedProtectedRoute as the only auth gate; remove page-level /api/auth/verify calls where context already has the data.

Does this mean removing middlewares from API?
I don't think so, cause the problem is that APIs for auth and other type of get requests are called many times.