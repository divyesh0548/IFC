@AI-Summary-API-Backend/run.py Currently the design gap analysis is giving very generic ouput and the eplaination is only one liner. So change the prompts so that ouput is more comprehensive, but not more then 3 sentences.

Also make a JSON file for each check's main prompt. So that I can chnage them easily.

Currently it just give exlpaination and recommendation. But I want these detaails. Alignment (Weak, Strong, partial), Alignment Rationale, Proposed solution.  

No need to chnage the DB columns, just store this in JSON format.

Also provide an option of dry run for development. In this user will click generate AI summary but user will get the input prompt generated by backend service in txt file. For this dry run just provide an bool in the flask app.

Also in reports the insufficient data is not detecting the fields and values. Many controls have empty values for "Key Control" column, but none of them are detected there. Fix that.

In Insufficient data, keep priority. 
First text validation. If that fails then do not feed to AI, just flag that control and do not run checks related to those missing fields.

Second AI based validation, in which AI will tell wehther the related fields has enough data or not. based on that flag that as insufficient data. this category doesn't count in design gaps.

For some reason, the insufficient data text validation is not working. Many controls has empty vlaues in "Key Control" column, but there are not flagged. Fix that.

Also ask for clarification or indicate any conflicts in this plan.