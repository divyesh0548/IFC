Fixes in coordinator module. 

In form details page do not display "Remarks by User" and "Reason by Approver" if they are empty. Only show those which are not null.

There is an issue in replicate function. It is not replicating cusomt added columns of base RACM. Just fixed columns are getting replicated. Fix that. All custom columns must be replicated because they are just read-only metadata fields.